<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.boardserver.mapper.CommentMapper">

    <!-- 댓글 등록 -->
    <insert id="register" parameterType="com.project.boardserver.dto.CommentDTO">
        INSERT INTO comment
            (postId,
             contents,
             subCommentId)
        VALUES (#{postId}, #{contents}, #{subCommentId})
    </insert>

    <!-- 댓글 수정 -->
    <update id="update" parameterType="com.project.boardserver.dto.CommentDTO">
        UPDATE comment
        SET contents = #{contents},
            subCommentId = #{subCommentId}
        WHERE id = #{id}
    </update>

    <!-- 댓글 삭제 -->
    <delete id="delete" >
        DELETE FROM comment
        WHERE id = #{commentId}
    </delete>

</mapper>