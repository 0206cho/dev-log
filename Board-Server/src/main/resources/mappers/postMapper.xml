<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.boardserver.mapper.PostMapper">

    <!-- 게시글 등록 -->
    <insert id="register" parameterType="com.project.boardserver.dto.PostDTO" >
        <selectKey resultType="int" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO post (name, isAdmin, contents, createTime, views, categoryId, userId, fileId, updateTime )
        VALUES (#{name}, #{admin}, #{contents}, #{createTime}, #{views}, #{categoryId}, #{userId}, #{fileId}, #{updateTime})
    </insert>

    <!-- 게시글 조회 - 본인 작성글-->
    <select id="selectMyPosts" resultType="com.project.boardserver.dto.PostDTO">
        SELECT `id`,
               `name`,
               `isAdmin`,
               `contents`,
               `createTime`,
               `views`,
               `categoryId`,
               `userId`,
               `fileId`,
               `updateTime`
        FROM `post`
        WHERE userId = #{userId}
    </select>

    <!-- 게시글 수정 -->
    <update id="update" parameterType="com.project.boardserver.dto.PostDTO">
        UPDATE post
        SET name = #{name},
            contents = #{contents},
            views = #{views},
            categoryId = #{categoryId},
            userId = #{userId},
            fileId = #{fileId},
            updateTime = #{updateTime}
        WHERE id = #{id}
    </update>

    <!-- 게시글 삭제 -->
    <delete id="delete" >
        DELETE FROM post
        WHERE id = #{productId}
    </delete>

</mapper>